{{- if .Values.externalServices.rds.enabled }}
{{- if .Values.externalServices.rds.primary }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.externalServices.rds.primary.name }}
  namespace: {{ .Values.namespace.name | default .Release.Namespace }}
  labels:
    app: {{ .Values.application.name | default .Chart.Name }}
    component: rds-primary
    env: {{ .Values.global.environment | default "prod" }}
    managed-by: helm
spec:
  type: ExternalName
  externalName: {{ .Values.externalServices.rds.primary.externalName }}
{{- end }}
{{- end }}
