apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "muze-project.fullname" . }}-test-connection"
  labels:
    {{- include "muze-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "muze-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
